# MkDocs
site/

# OS
.DS_Store
Thumbs.db

# Local tooling (commit .claude/settings.json so plugins load in web sessions)
.claude/*
!.claude/settings.json
.cursor/
.superpowers/

# Python
.venv/
__pycache__/
*.pyc

# RL training (gdrl / SB3 in repo root)
logs/
*.zip
*.onnx

# Stray default Godot project icon (this is a docs repo, not a Godot project)
icon.svg
icon.svg.import

# godot-native-rl addon — a pinned dependency, not course source.
# Install it with scripts/fetch-native-runner.sh (see GODOT_NATIVE_RL_VERSION).
examples/neural_foundations/game/addons/godot_native_rl/
